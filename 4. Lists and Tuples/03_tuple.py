# Tuple is a immutable data type in python
emptyTuple = () # creation of empty tuple
#! (1) is not considered as single element tuple and type will be considered as int
singleElementTuple = (1, ) # this is how we make empty tuple as we specified , after first element

a = (1, 2, 5, 6, False, "Shashwat")
print(type(a))
print(a)